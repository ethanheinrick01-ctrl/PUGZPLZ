# Exam 1 classroom edition — September 24, 2026

The September 24 formatting transcript controls this addition. The final Desktop Lecture 3 deck supplies the completed classroom corrections. Sutherland's DNA/Codex and the latest original question bank supply style; historical exam predictions do not override the current announcement.

## What is available

- Twelve focused mini-chapters covering foundations, audiograms, general inheritance, infections/causes, syndrome features and hearing-loss associations, aids/fitting, children, verification/validation, classroom acoustics/ALDs, outcomes/goals, count-the-dots and Dr. Lee's first lecture.
- 61 new questions added to the unchanged original 352; 413 total authored items, plus numerical/graph generators and eight new written prompts with explicit rubrics.
- Forms A/B/C and further generated forms: exactly 52 MC/T/F, two audiograms and two short answers. Four of the objective questions are first-lecture guest material. Forms can overlap because they share a bank.
- Immediate correction on every objective selection and every graph part. Per-question checking for numeric/multiple-response tasks. Per-question rubric reveal and honest self-rating for written work. Both the new and original practice/mock routes follow this feedback policy.
- Original images, source figures, textbook tables and relevant classroom handouts retained. Images are not removed for the classmate edition.

## Evidence boundaries

The announced total is 70 points, not 70 numbered tasks. Practice uses 52 one-point objectives, two six-point graphs and two three-point written answers: 56 numbered tasks, 70 points. The 6/20/22/4 objective split, exact MC/T/F mix, per-ear graph layout and individual task weights are practice design choices. The transcript does not specify them or an official time limit.

Syndrome incidence and syndrome-specific inheritance recall are excluded from this focused pool. General dominant/recessive/mitochondrial inheritance remains. The separate guest case-study lecture is excluded. Existing broader lab activities remain accessible with their original IDs.

Final Lecture 3 has 82 slides; SHA-256 `d61311176a7aec23f7d3885a33b5f980a506a9c9aa3c3002ff58b5accf0ed500`. Its stale filename still says “12 unresolved,” but its final classroom entries are filled. Keep the slide 62 optimal RT target (0.3 s) distinct from slide 66's small-room limit (0.6 s, 35 dBA). Shown textbook Tables 2-4 through 2-7 and the six-page count-the-dots article were reviewed, including their original figures.

The new lane explains Lecture 2's broader congenital/acquired terminology. It does not score disputed exclusive Turner hearing-loss types. Every new question retains source tags; original question data is byte-for-byte preserved.

## Persistence and score integrity

The storage key remains `comd4590-lab-v2`. The same saved attempts, concept IDs, Boss history, original mocks and active sessions survive normalization. No 6,000-attempt truncation remains. Exam 1 runs store snapshots of questions, first responses, explanations, drafts, rubric ratings, flags and position. Editing the question bank later does not change the question snapshot inside an existing run.

Before writing over existing data, the release creates a raw pre-update recovery copy. Export/import includes the new state and merges without duplicating attempts. Quota failures, corrupt JSON and competing-tab changes do not silently overwrite saved records. Use one active study tab; reload old tabs when updating. Keep downloaded backups before clearing browser data or moving to a different browser, computer, path or site. Storage is local, not account/cloud synchronization; separate browsers/computers have separate users' progress.

Mocks never grant mastery. First responses remain scored after feedback. Incomplete runs keep the full denominator. Writing points are visibly self-scored. Misses appear immediately and require fresh correct practice to clear; repeating one answer does not establish mastery.

## September 24 verification receipt

- All runtime JavaScript syntax checks: pass.
- Content validator: pass, 413 items, 101 existing concepts.
- Core unit suite: 42 passed, zero failed.
- Exam 1 suite: 16 passed. Includes 300 complete forms, 1,000 independently checked graph variants, first-response locks, scoring, miss remediation, old-state serialization/reload, export/import, separate stores and storage-failure safeguards.
- Original question data and every original image: hash-identical to the pre-extension version.
- CUA/Chrome preview: wrong answers immediately explained in the new mock, original mock and original practice. Graph selections explained and locked individually. Written draft/rubric, flag and question position survived actually closing and reopening the preview tab. A graded original practice answer survived reload without becoming answerable again. Source figures loaded; browser error log empty. Phone layout had no horizontal page overflow.
- The standalone terminal Playwright suite was updated for the new policy but was not executed in this session; browser verification used CUA instead.
- The existing user file-tab inspection was blocked by browser URL policy. No bypass or user-storage reset was attempted. Persistence verification used isolated test data; actual existing user progress was not read or claimed inspected. The canonical path/key are preserved, with automatic pre-update backup before the new release writes.

Historical `VALIDATION.md` describes earlier versions. This receipt supersedes its feedback-timing descriptions for this release.
